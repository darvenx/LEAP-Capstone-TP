Seed files for the trade database

-- Run `scripts/setup_db_and_seed.sh` with `TARGET_DATABASE` set (script will prompt for Postgres password), or run `scripts/apply_migrations.sh` then import CSVs manually.
-- Seed files are CSVs located in `seed/` and are loaded in the order required by foreign keys.
